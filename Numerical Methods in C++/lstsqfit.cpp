#include <iostream>
#include <cmath>
#include <fstream>

using namespace std;

int main(){

    int i,j,k,n;
    double xsum,ysum,xy,xsq;

    cout << "Enter the total number of data points :";
    cin >> n;

    double x[n],y[n];

    cout << "Enter the value of"
    for(i = 0; i < n; i++){
    cout <<"Enter the value of x["<< i << "]: ";
    cin >> x[i];
    }

    for(i = 0; i < n; i++){
    cout <<"Enter the value of y["<< i << "]: ";
    cin >> y[i];
    }










return 0;}
